{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for Watchfloor.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
